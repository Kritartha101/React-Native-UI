import { View, Text, StyleSheet } from "react-native";
import React from "react";

const Bottom = () => {
  return (
    <View style={{ marginTop: 20 }}>
      <Text style={{ fontSize: 2, color: "#fff", fontWeight: "bold" }}>
        Complete Profile
      </Text>
    </View>
  );
};

export default Bottom;

const styles = StyleSheet.create({});
